# AI-Powered-Resume-Reviewer.-
AI-powered resume reviewer that scores a resume against a specific job description across a weighted five-dimension rubric and returns prioritized, actionable fixes. Used Next.js 16, TypeScript, Tailwind, and Claude API . The score is computed deterministically in code from the model's per-dimension ratings, not guessed by the LLM.
